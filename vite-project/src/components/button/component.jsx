// import classNames from 'classnames'
// import styles from './styles.module.css'

// export const Button = ({ onClick, children}) => {
//     return <button className={classNames(styles.root)} onClick = {onClick}>{children}</button>
// } 